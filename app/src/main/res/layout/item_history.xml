<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:padding="10dp"
    android:background="#60FFFFFF"
    android:layout_height="match_parent">
    <!--标题，网址，注释，分类-->

    <ImageView
        android:id="@+id/iv_his_icon"
        android:layout_marginRight="10dp"
        android:src="@drawable/ic_web_2"
        android:layout_width="38dp"
        android:layout_height="38dp"
        android:scaleType="center"
        android:layout_alignTop="@+id/tv_his_title"
        android:layout_alignBottom="@+id/tv_his_url"
        />
    <TextView
        android:layout_toRightOf="@+id/iv_his_icon"
        android:id="@+id/tv_his_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="20dp"
        android:maxLines="1"
        android:layout_marginBottom="3dp"
        android:textColor="@color/textPrimary"
        android:text="文件名"/>
    <TextView
        android:id="@+id/tv_his_url"
        android:layout_alignLeft="@+id/tv_his_title"
        android:layout_below="@+id/tv_his_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="15dp"
        android:maxLines="1"
        android:textColor="@color/textSecondary"
        android:text="文件大小"/>
</RelativeLayout>